[pytest]
testpaths = tests
# test_medical_dataset.py requires the optional 'nibabel' package (NIfTI support).
# It is exercised only when nibabel is installed; skip collection otherwise so
# the default test run stays green on machines without it.
addopts = --ignore=tests/test_medical_dataset.py
python_files = test_*.py
